# Runtime dependencies (minimal) for the FastAPI backend.
# Keep this file lean for faster, more reliable Render builds.

fastapi==0.110.1
uvicorn[standard]==0.25.0

python-dotenv==1.2.1

motor==3.3.1
pymongo==4.5.0
dnspython==2.8.0

pydantic==2.12.5
email-validator==2.3.0
python-multipart==0.0.22

passlib[bcrypt]==1.7.4
python-jose==3.3.0
cryptography==46.0.4

PyPDF2==3.0.1

# Groq integration uses the official OpenAI Python client (OpenAI-compatible endpoint).
openai==1.99.9
